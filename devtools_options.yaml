# ═══════════════════════════════════════════════════════════════════════════
# 🔧 Bilgi Avcısı - DevTools Yapılandırması
# ═══════════════════════════════════════════════════════════════════════════
# Bu dosya Flutter DevTools için özel ayarları içerir.
# Daha fazla bilgi: https://docs.flutter.dev/tools/devtools
# ═══════════════════════════════════════════════════════════════════════════

description: Bilgi Avcısı DevTools yapılandırması.

extensions:
  # ─────────────────────────────────────────────────────────────────────────
  # Provider DevTools Extension
  # ─────────────────────────────────────────────────────────────────────────
  # Riverpod state'lerini izlemek için
  # Not: flutter_riverpod paketi otomatik olarak DevTools'a entegre olur
  
  # ─────────────────────────────────────────────────────────────────────────
  # Performance Overlay
  # ─────────────────────────────────────────────────────────────────────────
  # Debug modda performans overlay'ini etkinleştirmek için:
  # MaterialApp'te showPerformanceOverlay: true kullanın

# ═══════════════════════════════════════════════════════════════════════════
# Inspector Ayarları
# ═══════════════════════════════════════════════════════════════════════════
inspector:
  # Widget Inspector varsayılan ayarları
  defaultWidgetInspectorOptions:
    # Seçili widget'ın render object bilgisini göster
    showRenderObjectInfo: true
    # Widget sınırlarını göster
    showWidgetBounds: true

# ═══════════════════════════════════════════════════════════════════════════
# Logging Ayarları
# ═══════════════════════════════════════════════════════════════════════════
logging:
  # Log seviyesi (verbose, debug, info, warning, error)
  level: info
  
  # Filtrelenecek log kategorileri
  excludeCategories:
    - flutter.frame # Frame timing loglarını gizle
    - flutter.imageCache # Image cache loglarını gizle

# ═══════════════════════════════════════════════════════════════════════════
# Memory Profiler Ayarları
# ═══════════════════════════════════════════════════════════════════════════
memory:
  # GC olaylarını izle
  trackGC: true
  
  # Allocation tracking (performansı etkileyebilir)
  trackAllocations: false

# ═══════════════════════════════════════════════════════════════════════════
# Network Profiler Ayarları
# ═══════════════════════════════════════════════════════════════════════════
network:
  # HTTP isteklerini kaydet
  recordHttpRequests: true
  
  # WebSocket bağlantılarını izle
  trackWebSockets: true

# ═══════════════════════════════════════════════════════════════════════════
# CPU Profiler Ayarları
# ═══════════════════════════════════════════════════════════════════════════
cpuProfiler:
  # Profil örnekleme aralığı (mikrosaniye)
  samplePeriod: 250

# ═══════════════════════════════════════════════════════════════════════════
# App Size Analyzer
# ═══════════════════════════════════════════════════════════════════════════
# APK/IPA boyut analizi için:
# flutter build apk --analyze-size
# flutter build ipa --analyze-size
